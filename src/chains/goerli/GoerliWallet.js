const getPubBalance = async ({ address, w3 }) => {
}

const getLPBalance = async ({ address, w3 }) => {
}

const sendTokens = async ({ address, amount, w3 }) => {
}

export const GoerliWallet = {
  getPubBalance,
  getLPBalance,
  sendTokens,
}
